apiVersion: v1
kind: Service
metadata:
  name: gateway-master-node-port
  namespace: cornserve
spec:
  type: NodePort
  selector:
    app: gateway
    gateway-role: master
  ports:
  - name: gateway
    port: 8000
    targetPort: http
    nodePort: 30081
